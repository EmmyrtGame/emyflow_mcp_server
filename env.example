# Server Configuration
PORT=3000
NODE_ENV=development

# Database Connection
# MySQL connection string for Prisma
DATABASE_URL="mysql://user:password@localhost:3306/nexus_flow_db"

# Security
# Secret key for signing JWT tokens (Admin Panel Access)
JWT_SECRET="your-super-secret-jwt-key-change-this"
# Key for encrypting sensitive client credentials in the database
ENCRYPTION_KEY="your-32-char-encryption-key-hex"

# Integrations
# Webhook URL for Make.com / n8n agent handoff scenarios
MAKE_AGENT_WEBHOOK_URL="https://hook.us1.make.com/your-webhook-id"

# Optional: Global logging or monitoring keys can go here
